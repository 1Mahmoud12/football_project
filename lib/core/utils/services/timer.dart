/*

import 'dart:async';

import 'package:sofa_sccore/presentation/bloc/cubit.dart';

class TimerCount {
   Duration matchDuration = Duration.zero;
   Timer? countUpTimer;

   void startTimer(){

    TimerCount().countUpTimer=Timer.periodic(const Duration(seconds: 1), (timer) { checkCount();});
  }


*/
/*  static void stopTimer(){
    TimerCount.countUpTimer!.cancel();
   // emit(MatchesStopTimerState());
  }


  static void checkCount(){
    var plusSecondBy=1;
    int seconds =TimerCount.matchDuration.inSeconds+plusSecondBy;
    if(seconds>5450){
      TimerCount.countUpTimer!.cancel();
    }else{
      TimerCount.matchDuration=Duration(seconds: seconds);
    }

  }*//*

}
*/
